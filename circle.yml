machine:
  python:
    version: 3.5.1

dependencies:
  override:
    - pip install -r requirements.txt
    - pip install coveralls

test:
    override:
        - coverage run --source=sample_project.app manage.py test
            

    